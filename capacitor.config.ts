import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.snugglymongooseskip',
  appName: 'snuggly-mongoose-skip',
  webDir: 'dist'
};

export default config;
